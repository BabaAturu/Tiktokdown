# tiktok-downloader

Minimal scaffold for a TikTok downloader (backend + frontend).

Structure:

- backend/: Express API
- frontend/: React (Vite) app

Next steps:

1. Run `npm install` in `backend` and `frontend`.
2. Start backend: `npm start` (port 4000).
3. Start frontend: `npm run dev`.
